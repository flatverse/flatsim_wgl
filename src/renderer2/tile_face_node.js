flatsim.TileFaceNode = function (vert_start, face_start, colors_start, norms_start) {
  this.vert_start = vert_start;
  this.face_start = face_start;
  this.colors_start = colors_start;
  this.norms_start = norms_start;
};
